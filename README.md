# random-animals

Site that give you a random picture of cat or dog.

## Local use

If you want to start server locally, in your terminal write `npm start` and after that site will be available on `localhost:3000`.

---
